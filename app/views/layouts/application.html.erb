<!DOCTYPE html>
<html>
  <head>
    <title>TravelJournal</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    >
  </head>

  <body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to "Home",
        root_path,
        class: "navbar-brand text-decoration-underline" %>

    <div class="ms-auto">
      <% if @current_user %>
        <span class="navbar-text me-3">
          Logged in as <strong><%= @current_user.username %></strong>
        </span>
        <%= link_to "Logout",
            logout_path,
            method: :get,
            class: "nav-link d-inline text-decoration-underline" %>
    <% else %>
  <%= link_to "Login",
      login_path,
      class: "nav-link d-inline text-decoration-underline" %>
  <span class="mx-2">|</span>
  <%= link_to "Signup",
      signup_path,
      class: "nav-link d-inline text-decoration-underline" %>
<% end %>
  
    </div>
  </div>
</nav>


    <div class="container mt-4">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == "notice" ? "success" : "danger" %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>



